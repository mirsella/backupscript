[Unit]
Description=undervolt
After=network-online.target

[Service]
Type=exec
ExecStart=/usr/bin/undervolt --core -100 --cache -100 --gpu -40 --uncore -60 --analogio -60

[Install]
WantedBy=multi-user.target
